{{#if session.isAuthenticated }}

  {{#md-fixed-btns
    class="fixed-btns-example"
    btnIcon="mdi-content-add"
    btnClass="btn-large indigo"
    action="createTeam"
    actionArgs="0"}}

  {{/md-fixed-btns}}
{{/if}}
{{outlet}}
